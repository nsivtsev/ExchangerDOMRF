# Конвертер валют
Laravel + VueJS

Настроить соединение с БД в .env

Установка:
```
composer install
npm install
php artisan migrate
php artisan db:seed --class=CurrencySeeder
```

Запуск
```
php artisan serve
npm run build
```
